!!!
%html{lang: "ja"}
  %head
    %meta{charset: "utf-8"}
    %link{:href => "“rogin.scss”", :rel => "“stylesheet”"}/
  %body
    .all
      .contents
        .Touroku-Middle
          .Touroku-Sections
            .Touroku-Section ログイン画面
            .Mailform-Section
              .Touroku 
                %a.btn-default.btn-mail{:href => "/users/sign_up"}
                  アカウントをお持ちでない方はこちらから
        = form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
          .mail_field
            = f.label :メールアドレス
            %br/
            .mail_nyuryoku_area
              = f.email_field :email, autofocus: true, autocomplete: "email", class: 'email_zone', :placeholder => "PC携帯どちらもOK!"
          .password_field
            = f.label :パスワード
            %br/
            .password_nyuryoku_area
              = f.password_field :password, autocomplete: "current-password", class: 'password_zone', :placeholder => "半角英数字7文字以上"
          - if devise_mapping.rememberable?
            .field
              = f.check_box :remember_me
              = f.label :ログインしたままにする
          .actions
            = f.submit "ログイン", class:'rogin-btn'
= render "devise/shared/links"